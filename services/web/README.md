# web (Node / TypeScript service)

Node 24 + TypeScript application service.

```bash
npm install        # install deps
npm run dev        # run src/index.ts directly (type stripping)
npm run build      # compile to dist/
npm test           # node --test
npm run lint && npm run typecheck
```
